export type DateRageType = {
    startDate: string;
    endDate: string;
};

export type TotalCountInfomationType = {
    totalAdult: number;
    totalChild: number;
    totalAmount: number;
    totalOrder: number;
    totalPreOrder: number;
};
